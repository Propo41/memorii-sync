/**
 * Before triggering a sign-in request, you must initialize the Google SDK using your any required scopes and
 * the webClientId, which can be found in the android/app/google-services.json file as the client/oauth_client/client_id property
 * Make sure to pick the client_id with client_type: 3
 */

export const FIREBASE_WEB_CLIENT_ID = ''
export const SITE_URL = '';