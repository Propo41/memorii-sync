/**
 *  put the webclient id which can be found in the android/app/google-services.json file as the
 * client/oauth_client/client_id property (the id ends with .apps.googleusercontent.com).
 * Make sure to pick the client_id with client_type: 3
 */
export const firebaseWebClientId = '*.apps.googleusercontent.com';
